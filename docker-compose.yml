services:
  # 백엔드 컨테이너 서비스
  backend:
    build:
      context: ./back
      dockerfile: Dockerfile
    container_name: routecheck-backend
    ports:
      - "8000:8000"
    volumes:
      - ./back:/app
    environment:
      - TOUR_API_DECODE_KEY=${TOUR_API_DECODE_KEY}
      - KAKAO_REST_API_KEY=${KAKAO_REST_API_KEY:-}
      - TMAP_API_KEY=${TMAP_API_KEY:-}
      - ODSAY_API_KEY=${ODSAY_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - routecheck-network

  # 프론트엔드 컨테이너 서비스
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    container_name: routecheck-frontend
    ports:
      - "3000:3000"
    command: npm run dev
    volumes:
      - ./front:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NEXT_PUBLIC_KAKAO_MAP_KEY=${NEXT_PUBLIC_KAKAO_MAP_KEY}
      - NEXT_PUBLIC_GOOGLE_CLIENT_ID=${NEXT_PUBLIC_GOOGLE_CLIENT_ID}
      - NEXT_PUBLIC_NAVER_CLIENT_ID=${NEXT_PUBLIC_NAVER_CLIENT_ID}
      - BACKEND_HOST=backend
      - BACKEND_PORT=${BACKEND_PORT}
    depends_on:
      - backend
    networks:
      - routecheck-network

networks:
  routecheck-network:
    driver: bridge
