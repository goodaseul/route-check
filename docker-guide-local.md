# 🐳 RouteCheck 로컬 도커(Docker) 환경 가이드

본 문서는 프로젝트 루트 폴더에서 도커 환경을 초기 구동하는 순서와 개발 중 라이브러리(패키지)가 추가되었을 때 이미지를 재생성(재빌드)하는 방법을 안내합니다.

---

## 1. 도커 초기 세팅

### 1) 최상위 루트 폴더 이동 및 `.env` 확인

1. `docker-compose.yml` 파일이 위치한 **최상위 루트 폴더(`route-check`)**로 이동합니다.
2. `.env` 파일이 최상위 루트 폴더에 올바르게 위치해 있는지 확인합니다.

### 2) 최초 서버 빌드 및 구동

최상위 루트 폴더에서 아래 명령어를 입력하여 프론트엔드와 백엔드 이미지를 동시에 빌드하고 컨테이너를 가동합니다.

```bash
docker compose up --build
```

### 3) 구동 완료 후 브라우저 접속 주소

- Frontend: http://localhost:3000
- Backend (API): http://localhost:8000

---

## 2. 라이브러리 추가 시 이미지 재생성 가이드

개발 도중 프론트엔드나 백엔드에 새로운 외부 라이브러리가 추가되면, 도커 컨테이너 내부 환경도 이에 맞춰 동기화(이미지 재생성)해 주어야 합니다.

### 1) 내 파트는 내가 업데이트하고, 실행은 각자 명령어 한 줄로

깃에서 패키지 파일의 변경사항을 pull 받았다면, 본인 로컬 컴퓨터에서 서버를 켤 때 아래 가이드를 따릅니다.

- **프론트엔드 작업자**: front/package.json에 신규 npm 패키지 추가 후 깃 푸시
- **백엔드 작업자**: back/requirements.txt에 신규 파이썬 패키지 추가 후 깃 푸시

### 1) 깃에서 최신 코드를 받았는데 package.json이나 requirements.txt가 바뀌었을 때

새로운 라이브러리를 도커 이미지에 반영해야 하므로 반드시 뒤에 --build 옵션을 붙여서 실행합니다. 도커의 캐싱 기능 덕분에 처음부터 다 다운받지 않고 새로 추가된 라이브러리만 쏙 골라서 빠르게 빌드합니다.

```Bash
docker compose up --build
```

### 2) 라이브러리 추가 없이 그냥 소스 코드(JS, TS, PY)만 수정할 때

도커 컴포즈에 볼륨 마운트(volumes) 설정이 되어 있어서 로컬 소스 코드 수정 시 실시간 핫 리로딩(Fast Refresh)이 정상 작동합니다. 도커를 껐다 켤 필요 없이 평소처럼 켜놓고 개발하시면 됩니다.

```Bash
# 빌드 과정을 건너뛰고 기존 이미지를 사용해 1초 만에 서버 기동
docker compose up
```

### 3) 서버를 안전하게 종료하고 싶을 때

작업을 마치고 컨테이너를 내릴 때는 아래 명령어를 사용합니다.

```Bash
docker compose down
```

### 4) 도커 캐시가 꼬여서 신규 라이브러리가 자꾸 누락될 때

이전 빌드 파편이나 볼륨 캐시를 완전히 청소하고 제로 베이스에서 새로 빌드하는 명령어입니다.

```Bash
docker compose down
docker builder prune -a  # 도커 내 빌드 캐시 전면 청소
docker compose up --build
```
